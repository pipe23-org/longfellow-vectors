{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "common-v1.schema.json",
  "title": "longfellow-vectors shared value shapes, v1",
  "description": "Definitions referenced by the per-type vector schemas. Not a vector schema; no sidecar names this file in its `schema` field.",
  "$defs": {
    "hex": {
      "type": "string",
      "pattern": "^([0-9a-f]{2})+(?![\\s\\S])",
      "description": "Byte string as an even, positive number of lowercase hex digits."
    },
    "sha256hex": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}(?![\\s\\S])",
      "minLength": 64,
      "maxLength": 64,
      "description": "SHA-256 digest as 64 lowercase hex digits."
    },
    "p256coordinate": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}(?![\\s\\S])",
      "minLength": 64,
      "maxLength": 64,
      "description": "P-256 affine coordinate as 64 lowercase hex digits, left-padded with zeros."
    },
    "p256scalar": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}(?![\\s\\S])",
      "minLength": 64,
      "maxLength": 64,
      "description": "P-256 private scalar as 64 lowercase hex digits, left-padded with zeros."
    },
    "provenance": {
      "type": "object",
      "oneOf": [
        {
          "properties": {
            "type": { "const": "repository" },
            "repo": {
              "type": "string",
              "pattern": "^[^/\\s]+\\.[^/\\s]+/[^/\\s]+/[^/\\s]+(?![\\s\\S])",
              "description": "Source repository as host/owner/name, e.g. github.com/google/longfellow-zk."
            },
            "ref": {
              "type": "string",
              "pattern": "^[0-9a-f]{40}(?![\\s\\S])",
              "minLength": 40,
              "maxLength": 40,
              "description": "Full commit hash the bytes were copied out of."
            },
            "path": {
              "type": "string",
              "minLength": 1,
              "description": "Path of the source artifact within the repository at ref."
            }
          },
          "required": ["type", "repo", "ref", "path"],
          "additionalProperties": false
        },
        {
          "properties": {
            "type": { "const": "constructed" },
            "generator": {
              "type": "string",
              "minLength": 1,
              "description": "The generating command with every value the command generated filled in, e.g. generate.py flip-bit --proof <name> --byte 3. Re-running it reproduces the bytes for every command except proof."
            }
          },
          "required": ["type", "generator"],
          "additionalProperties": false
        }
      ]
    }
  }
}
